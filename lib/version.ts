// The project's version and stage, shown in the footer. A project people can
// see is expected to say where it is, so this is the one place that answers it.
//
// APP_VERSION tracks package.json's version; bump both together. APP_STAGE is
// the current roadmap phase in plain words, so a visitor knows this is work in
// progress rather than a finished product.
export const APP_VERSION = "0.1.4";
export const APP_STAGE = "Phase 4 preview";
